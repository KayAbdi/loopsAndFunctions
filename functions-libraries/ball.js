var list = [];

list.push(create());
list.push(create());
list.push(create());
list.push(create());

var i = 0;
undefined
var length = list.length;

while(i < length){
    var ball = list[i];
    colorRandom(ball);
    i++
}


while(i < length){
    var ball = list[i];
    colorRandom(ball);
    i++
};
